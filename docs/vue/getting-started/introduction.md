---
title: Introduction
description: Ez Scrollbar is a simple, customizable scrollbar library for Vue. Add styled scrollbars to any scrollable area with minimal code.
---

# Introduction

**Ez Scrollbar** is a small library that gives you custom scrollbars in your Vue app. If you've ever wanted scrollbars that match your design (or that work the same everywhere), this library is for you.

## Why use a custom scrollbar?

- **Consistent look** — Native scrollbars look different in Chrome, Firefox, Safari, and on Windows vs macOS. With Ez Scrollbar, you control the appearance.
- **Design match** — Style the track and thumb to fit your app (colors, size, rounded corners, etc.).
- **Same API everywhere** — One component works across browsers and platforms.

## Who is this for?

Ez Scrollbar is for you if:

- You're building a Vue 3 app and want scrollbars that look good and behave the same everywhere.
- You're new to Vue and want something that "just works" with a simple `<ScrollBar>` wrapper.
- You need more control later — you can use the provided composables to build your own scrollbar UI.

You don't need to be an expert. The next page will walk you through installing and your first scrollable area.

## What you'll need

- A Vue 3 project (Vite, Vue CLI, or any other setup).
- A few minutes to install and try the basic example.

Ready? Head to [Installation](/vue/getting-started/installation) to add the package and wrap your first scrollable content.
